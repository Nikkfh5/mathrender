# MathRender Claude Guide

Use `AGENTS.md` as the source of truth for project workflow, tests, release rules, and supported-agent positioning.

Claude Code remains supported through the auto-installed `~/.claude/settings.json` Stop hook, but MathRender should not be described as Claude-only. The product target is Codex plus Claude Code via the shared `hook_send_formulas.py` adapter.
